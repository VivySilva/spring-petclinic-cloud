1. Pré-requisitos
Docker Desktop rodando
Kubernetes ativado no Docker Desktop
kubectl version --client

kubectl funcionando
kubectl get nodes

helm instalado
helm version

mvn (Maven) instalado
mvn -version


2. Definir onde as imagens Docker vão ficar
O Kubernetes não builda código, ele puxa imagens prontas. Então precisamos publicar as imagens do PetClinic em um repositório Docker.

docker login
export REPOSITORY_PREFIX=vivysilva


3. Buildar as imagens dos microserviços
O Maven vai usar Buildpacks para criar as imagens Docker de cada microserviço.

cd "C:\Users\vivys\Documents\spring-petclinic-cloud"
mvn spring-boot:build-image -Pk8s -DREPOSITORY_PREFIX=$env:REPOSITORY_PREFIX ou mvn spring-boot:build-image -Pk8s -DREPOSITORY_PREFIX=vivysilva
./scripts/pushImages.sh


4. Criar o namespace no Kubernetes
Namespace é como se fosse uma “pasta lógica” dentro do cluster.

kubectl apply -f k8s/init-namespace/
kubectl get ns # Verificar


5. Criar os serviços internos
Cria-se os endereços internos que os microsserviços usam para se comunicar.

kubectl apply -f k8s/init-services
kubectl get svc -n spring-petclinic # Verificar


6. Subir os bancos MySQL
Cada microserviço tem seu próprio banco. Então precisa-se criar 3 bancos.
No README pede-se para usar as imagens do Bitnami, mas não estão mais disponíveis. Então, foi criado o mysql.yaml para subistituí-lo.

kubectl apply -f mysql.yaml
kubectl get pods -n spring-petclinic


7. Fazer deploy dos microserviços
kubectl apply -f k8s/ -n spring-petclinic

Se der erro de CreateContainerConfigError:
Customers DB:
kubectl delete secret customers-db-mysql -n spring-petclinic
kubectl create secret generic customers-db-mysql `
  --from-literal=mysql-root-password=petclinic `
  -n spring-petclinic
kubectl rollout restart deployment customers-service -n spring-petclinic

Vets DB:
kubectl delete secret vets-db-mysql -n spring-petclinic
kubectl create secret generic vets-db-mysql `
  --from-literal=mysql-root-password=petclinic `
  -n spring-petclinic
kubectl rollout restart deployment vets-service -n spring-petclinic

Visits DB:
kubectl delete secret visits-db-mysql -n spring-petclinic
kubectl create secret generic visits-db-mysql `
  --from-literal=mysql-root-password=petclinic `
  -n spring-petclinic
kubectl rollout restart deployment visits-service -n spring-petclinic

Se não ficarem Ready é pq não existe o Config Server e Discovery Server:
docker build -t config-server:latest ./spring-petclinic-config-server
docker build -t discovery-server:latest ./spring-petclinic-discovery-server
kubectl apply -f k8s/discovery-server.yaml
kubectl apply -f k8s/config-server.yaml
kubectl rollout restart deployment customers-service -n spring-petclinic
kubectl rollout restart deployment vets-service -n spring-petclinic
kubectl rollout restart deployment visits-service -n spring-petclinic
kubectl rollout restart deployment api-gateway -n spring-petclinic

REBUILDAR
cd spring-petclinic-vets-service 
mvn clean package -DskipTests 
cd .. 
docker build -t vivysilva/vets-service:latest . 
docker push vivysilva/vets-service:latest 
kubectl rollout restart deployment vets-service -n spring-petclinic

cd spring-petclinic-customers-service 
mvn clean package -DskipTests 
cd .. 
docker build -t vivysilva/customers-service:latest . 
docker push vivysilva/customers-service:latest
kubectl rollout restart deployment customers-service -n spring-petclinic

cd spring-petclinic-visits-service 
mvn clean package -DskipTests 
cd .. 
docker build -t vivysilva/visits-service:latest . 
docker push vivysilva/visits-service:latest
kubectl rollout restart deployment visits-service -n spring-petclinic

cd spring-petclinic-api-gateway 
mvn clean package -DskipTests 
cd .. 
docker build -t vivysilva/api-gateway:latest . 
docker push vivysilva/api-gateway:latest
kubectl rollout restart deployment api-gateway -n spring-petclinic

kubectl delete deployment wavefront-proxy -n spring-petclinic
sudo k3s kubectl delete deployment wavefront-proxy -n spring-petclinic

kubectl exec -it mysql-785d9c5794-dbqpk -n spring-petclinic -- mysql -u root -p

CREATE DATABASE petclinic;
CREATE DATABASE vets;
CREATE DATABASE visits;
CREATE DATABASE customers;

GRANT ALL PRIVILEGES ON petclinic.* TO 'petclinic'@'%';
GRANT ALL PRIVILEGES ON vets.* TO 'petclinic'@'%';
GRANT ALL PRIVILEGES ON visits.* TO 'petclinic'@'%';
GRANT ALL PRIVILEGES ON customers.* TO 'petclinic'@'%';

FLUSH PRIVILEGES;

ou 

CREATE DATABASE IF NOT EXISTS petclinic;

CREATE USER IF NOT EXISTS 'petclinic'@'%' IDENTIFIED BY 'petclinic';

GRANT ALL PRIVILEGES ON petclinic.* TO 'petclinic'@'%';

FLUSH PRIVILEGES;

exit;

kubectl rollout restart deployment customers-service -n spring-petclinic
kubectl rollout restart deployment vets-service -n spring-petclinic
kubectl rollout restart deployment visits-service -n spring-petclinic
kubectl rollout restart deployment api-gateway -n spring-petclinic

Comandos para acessar as ferramentas
Prometheus: kubectl port-forward svc/prometheus -n spring-petclinic 9090:9090
Loki: kubectl port-forward svc/loki -n spring-petclinic 3100:3100
Alloy (browser): kubectl port-forward -n spring-petclinic daemonset/promtail 9080:9080
Zipkin: kubectl port-forward svc/zipkin -n spring-petclinic 9411:9411
Api-gateway: kubectl port-forward svc/api-gateway -n spring-petclinic 8080:80

Logs recentes:
$query = [uri]::EscapeDataString('{namespace="spring-petclinic"}')
Invoke-RestMethod -Uri "http://localhost:3100/loki/api/v1/query_range?query=$query&limit=20" | ConvertTo-Json -Depth 5

Traces recentes:
$traces = Invoke-RestMethod -Uri "http://localhost:9411/api/v2/traces?limit=10"
$traces | ConvertTo-Json -Depth 10

Métricas dos pods recentes:
Invoke-RestMethod -Uri "http://localhost:9090/api/v1/label/__name__/values" | ConvertTo-Json -Depth 3
Invoke-RestMethod -Uri "http://localhost:9090/api/v1/targets" | ConvertTo-Json -Depth 8

//----------------------------------------------------------------------------

Comandos no wsl/Ubutu

cd /mnt/c/Users/vivys/Documents/spring-petclinic-cloud

Iniciar a aplicação:
1	Todas (Ubuntu/WSL) - sudo systemctl start tailscaled && sudo tailscale up
2	Notebook-Vivy - sudo systemctl start k3s
3	Augusto + Mac - sudo systemctl start k3s-agent
4 Recriar o bd, caso os pods não subam

Veirificar os nós:
sudo k3s kubectl get nodes
sudo k3s kubectl get nodes -o wide

Verificar os pods:
sudo k3s kubectl get pods -n spring-petclinic -o wide

Reiniciar os pods:
sudo k3s kubectl rollout restart deployment api-gateway customers-service vets-service visits-service -n spring-petclinic

Iniciar o experimento:
bash run_experiment.sh

Realizar o Pré Porcessamento:
python preprocess.py --min_date 2026-06-01

Deligar/Parar a aplicação:
1 Augusto + Mac - sudo systemctl stop k3s-agent
2 Notebook-Vivy - sudo systemctl stop k3s